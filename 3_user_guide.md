---
marp: true
theme: default
paginate: true
---

# agile-board 使い方ガイド

チーム開発の進捗・工数・振り返りをまとめて管理

---

## agile-board でできること

```
スプリントの進捗を        バーンダウンチャートで
リアルタイムに確認        一目で把握

作業実績を               エピック（案件）単位で
カレンダーで記録          工数を集計・CSV出力

KPTで振り返りを           マイルストーンで
チームで蓄積              中長期の計画を管理
```

---

## 画面マップ

| 画面 | できること |
|---|---|
| **ダッシュボード** | 今のスプリントの進捗をひと目で確認 |
| **スプリント** | Issue のポイント入力・バーンダウン確認 |
| **エピック** | 案件ごとの進捗・工数を管理 |
| **マイルストーン** | 月次目標の管理・スプリントとの紐付け |
| **レトロスペクティブ** | KPT（振り返り）の記録・履歴確認 |
| **実績入力** | 週次タイムラインで作業時間を記録 |
| **勤怠管理** | 休暇・早退・遅刻を週単位で記録 |

---

# ダッシュボード

---

## ダッシュボード — 確認できること

**URL**: `/dashboard`

ログイン後のトップ画面。現在進行中のスプリントの状況を表示します。

```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│  総ポイント  │ 完了ポイント │  残ポイント  │   残日数    │
│     40      │     25      │     15      │    3日      │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

- **バーンダウンチャート**: 理想の消化ペース vs 実際の消化ペースを比較
- **KPT サマリー**: 今のスプリントに登録された振り返りの件数
- **進行中 Issue**: まだ完了していない Issue の一覧

---

# スプリント

---

## スプリント一覧でできること

**URL**: `/sprints`

チームのスプリント（開発期間）の一覧が表示されます。

- 進行中・完了済みのスプリントをひと目で確認
- 各スプリントの**ポイント速度**・**Issue 完了数**を確認
- スプリント名をクリックして詳細へ

---

## スプリント詳細 — Issue タブ

**URL**: `/sprints/{id}`

スプリントに含まれる Issue（タスク）の一覧です。

**Story（親 Issue）でできること**
- ストーリーポイントの確認
- **エピック（案件）との紐付け**: ドロップダウンから選択するだけ

**Task（Sub-issue）でできること**
- **予定工数の入力**: 数値を入力してフォーカスを外すと自動保存
- **実績工数の確認**: ワークログから自動集計された値を表示
- 「記録」リンクからワークログ入力画面へ直接移動

---

## スプリント詳細 — バーンダウンタブ

ポイントの消化状況をグラフで確認できます。

```
ポイント
  40 |＼ ← 理想線（グレー点線）
  30 | ＼
  20 |  ＼___  ← 実績線（青実線）
  10 |      ＼____
   0 +──────────────→ 日付
    開始日           終了日
```

- 実績線が理想線より**上 → 遅れ気味**
- 実績線が理想線より**下 → 順調**

---

## スプリント詳細 — 担当者タブ

チームメンバーごとの**負荷バランス**を確認できます。

| 表示内容 | 説明 |
|---|---|
| オープン Issue 数 | 各担当者のまだ完了していない Issue 件数 |
| 合計ポイント | 各担当者が担当する未完了 Issue のポイント合計 |

特定のメンバーに負荷が集中していないかをチェックするのに便利です。

---

# エピック（案件）

---

## エピック一覧でできること

**URL**: `/epics`

案件（大きな機能単位）ごとの進捗・工数をまとめて確認できます。

| 表示情報 | 内容 |
|---|---|
| **進捗バー** | 完了 Issue 数 ÷ 全 Issue 数 |
| **ステータス・優先度** | GitHub Projects の Status・Priority を自動反映 |
| **着手日目安**（青） | リリース日・工数から逆算した目安（自動計算） |
| **着手日**（緑） | 実際の着手日 |
| **実績 / 予定（h）** | タスク工数のサマリー |
| **推定スプリント数** | 残ポイントから算出した残り期間の目安 |

---

## エピック — 新規作成

「**+ 新規エピック（案件）**」ボタンから作成:

| 入力項目 | 説明 |
|---|---|
| タイトル | 案件名（必須） |
| 説明 | 概要・目的 |
| リリース予定日 | 目標日を設定すると着手日目安が自動計算される |
| 着手日 | 実際に着手した日（後から変更可） |

> ステータスと優先度は GitHub Projects と同期して自動設定されます

---

## エピック — Issue との紐付け

Issue（Story）とエピックの紐付けは**スプリント詳細画面**から行います。

1. `/sprints/{id}` を開き「Issue 一覧」タブを表示
2. 紐付けたい Issue 行の**エピック列ドロップダウン**をクリック
3. 一覧からエピックを選択 → すぐに保存される
4. 解除する場合は「案件なし」を選択

---

## 工数 CSV エクスポート

エピック × 担当者ごとの工数を CSV でダウンロードできます。

1. エピック一覧右上で**集計期間**を選択（デフォルト: 当月）
2. 「**CSV エクスポート**」ボタンをクリック

```
案件名,担当者,予定工数(h),実績工数(h),予定工数(人日),実績工数(人日)
案件A,alice,10,8,1.43,1.14
案件B,bob,20,18,2.86,2.57
```

> クライアントへの月次報告や工数集計に活用できます

---

# マイルストーン

---

## マイルストーンでできること

**URL**: `/milestones`

月次単位でプロジェクトの目標・進捗を管理します。

- 「今月・今後」タブと「過去」タブで切り替え表示
- 各マイルストーンに**ゴール**・**ステータス**・**期限**を設定
- **配下のスプリント**を紐付けて月次の達成状況を把握

> マイルストーンは現在月から前後の月が自動的に作成されます（手動作成・削除は不可）

---

## マイルストーン詳細・編集

**詳細ページ**（`/milestones/{id}`）でできること:
- 配下のスプリント一覧と集計の確認
- 「スプリントを追加」ボタンでスプリントを紐付け
- 各スプリント行の「解除」ボタンで紐付けを外す

**編集ページ**（`/milestones/{id}/edit`）で変更できる項目:

| 項目 | 説明 |
|---|---|
| タイトル | マイルストーン名（例: 「4月リリース」） |
| ゴール | 月の目標・説明文 |
| ステータス | 計画中 / 進行中 / 完了 |
| 着手日・期限 | 実際の開始日・終了目標日 |

---

# レトロスペクティブ

---

## レトロスペクティブでできること

**URL**: `/retrospectives`

スプリントごとに KPT（振り返り）を記録・管理します。

**KPT 入力タブ**（選択中のスプリント）

| 種別 | 色 | 使い方 |
|---|---|---|
| **Keep** | 緑 | うまくいったこと・次も続けたいこと |
| **Problem** | 赤 | 問題・課題・改善したいこと |
| **Try** | 青 | 次のスプリントで試してみること |

**履歴タブ**: 過去の全スプリントの KPT を横並びで一括確認

---

## KPT の追加・編集・削除

**追加**:
1. スプリントをドロップダウンで選択
2. 種別（Keep / Problem / Try）を選択
3. 内容を入力して「追加」をクリック

**編集**: カードの「編集」ボタン → テキストエリアが展開 → 「保存」で確定

**削除**: カードの「削除」ボタン → 確認ダイアログが表示

---

# 実績入力（ワークログ）

---

## ワークログでできること

**URL**: `/work-logs`

**週次タイムラインカレンダー**で作業実績を記録します。

```
        月   火   水   木   金   土   日
 9:00 │     │    │    │    │    │    │
10:00 │▓▓▓▓│▓▓▓▓│    │    │    │    │
11:00 │▓▓▓▓│▓▓▓▓│▓▓▓▓│    │    │    │
12:00 │    │    │    │    │    │    │
```

- 土日・祝日は背景色でハイライト表示
- 画面上部の「<」「>」ボタンで前後の週に移動
- メンバーフィルタで他のメンバーの実績も確認可能

---

## ワークログの記録方法

**新規登録**:
1. カレンダー上の**時間帯をドラッグ**して時間枠を選択
2. モーダルに必要事項を入力して登録

| 入力項目 | 説明 |
|---|---|
| 実績種別 | 作業の種類（開発・会議・調査など） |
| 開始 / 終了時刻 | 時間枠から自動入力される |
| エピック | 関連する案件（任意） |
| イシュー | 関連する Issue（任意） |
| 備考 | メモ・補足（任意） |

**移動**: 登録済みブロックをドラッグで時間を変更
**リサイズ**: ブロック下端をドラッグで終了時刻を変更

---

## ワークログと工数の連携

エピック・スプリント画面にワークログの実績が自動反映されます。

```
ワークログに作業時間を記録
        ↓
スプリント詳細の Task 行に「実績(h)」として表示
        ↓
エピック一覧の「実績 / 予定（h）」に集計
        ↓
CSV エクスポートで月次工数レポートとして出力
```

---

# 勤怠管理

---

## 勤怠管理でできること

**URL**: `/attendance`

個人の勤怠記録（休暇・早退・遅刻）を週単位で管理します。

| 種別 | 説明 |
|---|---|
| 全休 | 終日休み |
| 午前半休 | 午前中のみ休み |
| 午後半休 | 午後のみ休み |
| 早退 | 指定した時刻以降に早退 |
| 遅刻 | 指定した時刻まで遅刻 |

---

## 勤怠の登録・削除

**登録**:
1. 記録したい日付の「**+**」ボタンをクリック
2. 種別を選択（早退・遅刻は時刻も入力）
3. 「保存」をクリック

> 同じ日にすでに記録がある場合は上書きされます

**削除**: 記録済みバッジの「**×**」ボタンをクリック

---

# 設定

---

## 設定画面でできること

サイドバーの「設定」から各設定ページへ移動できます。

| 設定ページ | できること |
|---|---|
| **一般** | 外観（ライト/ダーク）・1人日の基準時間・ワークログの定時設定 |
| **リポジトリ** | 同期する GitHub リポジトリの追加・有効化 |
| **メンバー** | 表示名・1日の稼働時間の設定 |
| **ラベル** | ベロシティ計算から除外するラベルの設定 |
| **実績種別** | ワークログで使用するカテゴリの管理 |
| **祝日** | 祝日・特別休日の管理（着手日目安の計算に使用） |

---

## よく使う設定

**外観の切り替え** → 設定 → 一般 → ライト / ダーク / システム連動

**メンバーの稼働時間設定** → 設定 → メンバー → 「編集」→ 1日の稼働時間を入力
（エピックの着手日目安の計算に使われます）

**祝日の自動取得** → 設定 → 祝日 → 「日本の祝日を自動取得」ボタン

**ワークログのカテゴリ追加** → 設定 → 実績種別 → グループとカテゴリを管理

---

## まとめ

```
毎日の流れ:
  ワークログ で作業実績を記録
  スプリント でポイントと工数の確認

週次の流れ:
  GitHub 同期 → バーンダウン確認
  → KPT で振り返り入力

月次の流れ:
  マイルストーンのステータス更新
  → エピックから工数 CSV をエクスポート
```

---

## 困ったときは

| 症状 | 確認場所 |
|---|---|
| バーンダウンが表示されない | スプリントに「開始日」が設定されているか確認 |
| メトリクスがすべて 0 | Issue にストーリーポイントが設定されているか確認 |
| Issue がスプリントに表示されない | GitHub 同期を実行 |
| 着手日目安が表示されない | エピックに「リリース予定日」・Task に「予定工数」が設定されているか確認 |
| ワークログが実績に反映されない | 実績種別のグループが「工数あり」になっているか確認 |
