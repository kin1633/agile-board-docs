---
marp: true
theme: default
paginate: true
---

# agile-board 使い方ガイド

セットアップから各画面の操作手順まで — エンジニア向け

---

## 目次

1. ローカル環境構築
2. GitHub OAuth 設定
3. リポジトリ・メンバー設定
4. GitHub 同期
5. ダッシュボード
6. スプリント管理
7. エピック（案件）管理
8. マイルストーン
9. レトロスペクティブ
10. 実績入力（ワークログ）
11. 勤怠管理
12. 設定

---

# セットアップ

---

## ローカル環境構築（1/2）

```bash
# 1. クローン
git clone https://github.com/your-org/agile-board.git
cd agile-board

# 2. 依存関係インストール
composer install
npm install

# 3. 環境変数設定
cp .env.example .env
php artisan key:generate
```

**必要なもの**: PHP 8.3+, Composer, Node.js 22+, npm

---

## ローカル環境構築（2/2）

```bash
# 4. DB マイグレーション
php artisan migrate

# 5. フロントエンドビルド
npm run build

# 6. 開発サーバー起動
composer run dev
```

ブラウザで `http://localhost:8000` を開き、
「GitHub でログイン」が表示されれば成功

---

## GitHub OAuth 設定

`.env` に以下を設定:

```env
APP_URL=http://localhost:8000

GITHUB_CLIENT_ID=your_client_id
GITHUB_CLIENT_SECRET=your_client_secret
GITHUB_REDIRECT_URI=http://localhost:8000/auth/github/callback
```

GitHub OAuth App の作成:
1. GitHub → Settings → Developer settings → OAuth Apps
2. Authorization callback URL = `{APP_URL}/auth/github/callback`
3. Client ID / Secret を `.env` にコピー

---

## リポジトリの追加

**URL**: `/settings/repositories`

1. 「+ リポジトリを追加」をクリック
2. **オーナー名**（例: `octocat`）と**リポジトリ名**を入力
3. 追加後、「有効にする」で `active = true` に切り替え

| 設定 | 説明 |
|---|---|
| **有効** | GitHub 同期の対象になる |
| **無効** | 同期をスキップ |
| **Project #** | GitHub Projects v2 のプロジェクト番号（Iteration モード用） |

---

## Iteration モードの有効化

GitHub Projects v2 でスプリントを管理する場合:

1. `/settings/repositories` でリポジトリの `Project #` を入力
   - GitHub Projects URL の末尾の数字（例: `.../projects/5` → `5`）
2. **一度ログアウト → 再ログイン**して `project` スコープを取得
3. GitHub Projects に `Sprint`（週次）の Iteration フィールドを作成

> フィールド名（デフォルト: `Sprint`）は `settings` テーブルの `sprint_iteration_field` で変更可

---

## メンバー登録

**URL**: `/settings/members`

- メンバーは **GitHub OAuth でログインした時点で自動登録**
- 手動追加・削除は不可

各メンバーの編集で設定できる項目:

| 項目 | 説明 | デフォルト |
|---|---|---|
| 表示名 | アプリ内表示名 | GitHub ログイン名 |
| 1日の稼働時間 | 工数グラフ・着手日目安の計算に使用 | 6時間/日 |

---

# GitHub 同期

---

## GitHub 同期の実行

ナビバー右上の「**GitHub 同期**」ボタンをクリック

同期内容:
- スプリント（Iteration モードのみ: GitHub Projects の Sprint Iteration → スプリント）
- Issue（Story + Task/Sub-issues）
- ラベル
- Epic の着手日自動設定

> マイルストーンは**同期対象外**（アプリ独自管理）
> 定期自動同期は未実装。必要に応じて手動実行

---

## よくある同期エラー

| 症状 | 原因 | 対処 |
|---|---|---|
| Projects 権限エラー | `project` スコープがない | 再ログインしてスコープ取得 |
| プライベートリポジトリが同期されない | `repo` スコープ不足 | 再ログインしてスコープ確認 |
| Sub-issues が取得できない | Sub-issues Public Preview API 未対応環境 | GraphQL-Features ヘッダーが必要（実装済み） |
| 同期は成功するが Issue が増えない | リポジトリが inactive | `/settings/repositories` で有効化 |

---

# ダッシュボード

---

## ダッシュボード概要

**URL**: `/dashboard`（現在進行中スプリントを自動表示）

4つのメトリクスカード:

| カード | 内容 |
|---|---|
| **総ポイント** | スプリント内の全 Issue の story_points 合計 |
| **完了ポイント** | クローズされた Issue のポイント合計 |
| **残ポイント** | 総ポイント − 完了ポイント |
| **残日数** | 今日からスプリント終了日までの日数 |

---

## ダッシュボード — バーンダウン・KPT

**バーンダウンチャート**
- **理想線（グレー点線）**: start_date から working_days で均等消化
- **実績線（青実線）**: Issue の `closed_at` 基づく残ポイント推移

> `start_date` が未設定のスプリントではチャートが表示されません

**KPT サマリー**
現在のスプリントに登録されている Keep / Problem / Try の件数を表示

**進行中 Issue**
現在スプリントのオープン Issue 一覧（番号・タイトル・担当者・ポイント）

---

# スプリント管理

---

## スプリント一覧

**URL**: `/sprints`

各スプリントに表示される情報:

| 項目 | 説明 |
|---|---|
| タイトル | スプリント名（GitHub Sprint Iteration 名） |
| 状態バッジ | `open`（緑）/ `closed`（グレー） |
| 期間 | `start_date` 〜 `end_date` ・ 稼働日数 |
| ポイントベロシティ | closed Issue の story_points 合計 |
| Issue ベロシティ | closed Issue 件数 |

---

## スプリント詳細 — Issue タブ

**URL**: `/sprints/{id}`

各 Story 行で設定できる項目:

| 項目 | 説明 |
|---|---|
| **story_points** | ストーリーポイント（ベロシティ計算に使用） |
| **epic 紐付け** | ドロップダウンでエピックを選択（即時保存） |

各 Task（Sub-issue）行で設定できる項目:

| 項目 | 説明 |
|---|---|
| **予定 (h)** | 予定工数を直接入力（フォーカス外れで自動保存） |
| **実績 (h)** | ワークログ集計値（読み取り専用）・消化率バッジ表示 |
| **記録リンク** | ワークログ入力画面に遷移 |

---

## スプリント詳細 — バーンダウン・担当者タブ

**バーンダウンタブ**
- 理想線 vs 実績線のポイント消化グラフ
- `story_points` 設定 Issue のみ対象

> 開始日・稼働日数は現在 tinker で手動設定
> ```bash
> php artisan tinker --execute "
> App\Models\Sprint::find(1)->update([
>     'start_date' => '2025-04-01', 'working_days' => 10
> ]);"
> ```

**担当者別タブ**
担当者ごとのオープン Issue 数・合計ポイントの棒グラフ（チーム負荷確認用）

---

# エピック（案件）管理

---

## エピック一覧

**URL**: `/epics`

各エピックに表示される情報:

| 項目 | 説明 |
|---|---|
| GitHub ステータス / 優先度バッジ | GitHub Projects の Status / Priority フィールド値を自動反映 |
| リリース予定日 + 残り日数 | 残り日数に応じて色変化 |
| 着手日目安（青） | due_date・バッファ・工数から逆算（自動計算） |
| 着手日（緑） | 実際の着手日（手動 or 同期時自動設定） |
| 進捗バー | closed / 全 Issue 数 % |
| 実績 / 予定（h） | Task 工数サマリー（ワークログ合計 / 予定工数合計） |
| 推定スプリント数 | 残ポイント ÷ 平均ベロシティ |

---

## エピックの作成・編集

「+ 新規エピック（案件）」ボタンから作成:

| 項目 | 必須 | 説明 |
|---|---|---|
| タイトル | ✅ | エピック名 |
| 説明 | — | 概要・目的 |
| リリース予定日 | — | `YYYY-MM-DD` |
| 着手日 | — | 手動設定（同期でも自動設定される） |

> ステータス・優先度は GitHub Projects 同期で自動設定されるため、作成・編集フォームには含まれない

---

## Issue とエピックの紐付け

GitHub 同期では紐付けは**行われない**。UI から手動で設定:

1. `/sprints/{id}` の Issue タブを開く
2. 紐付けたい Issue 行のエピック列ドロップダウンをクリック
3. 一覧からエピックを選択 → 即時保存
4. 解除したい場合は「案件なし」を選択

---

## 工数 CSV エクスポート

**URL**: `GET /epics/export?from=YYYY-MM-DD&to=YYYY-MM-DD`

1. エピック一覧右上で集計期間を入力（デフォルト: 当月）
2. 「CSV エクスポート」ボタンをクリック

CSV フォーマット:
```
案件名,担当者,予定工数(h),実績工数(h),予定工数(人日),実績工数(人日)
案件A,alice,10,8,1.43,1.14
```

> 期間フィルタ: Task の `closed_at` が指定期間内のもののみ集計
> Excel 文字化け防止のため UTF-8 BOM 付き

---

# マイルストーン

---

## マイルストーン一覧

**URL**: `/milestones`

「今月・今後」タブ（デフォルト）と「過去」タブの2タブ構成:

| 項目 | 説明 |
|---|---|
| 年月 | `2026年4月` などの形式 |
| タイトル | マイルストーン名 |
| ステータス | `planning`（青）/ `in_progress`（黄）/ `done`（緑） |
| 期限 | `due_date`（未設定なら「—」） |
| スプリント数 | 紐付けられているスプリント件数 |

> アクセス時に現在月 −6〜+12ヶ月（計19ヶ月）が自動補完される（手動作成・削除は不可）

---

## マイルストーンとスプリントの紐付け

マイルストーン詳細（`/milestones/{id}`）から操作:

- **紐付け**: 「スプリントを追加」ボタン → 未割当スプリント一覧モーダルから選択
- **解除**: 各スプリント行の「解除」ボタン

編集できる項目（`/milestones/{id}/edit`）:

| 項目 | 説明 |
|---|---|
| タイトル | マイルストーン名 |
| ゴール | 目標・説明 |
| ステータス | planning / in_progress / done |
| 着手日・期限 | 手動設定 |

---

# レトロスペクティブ

---

## KPT ボード

**URL**: `/retrospectives`

スプリント選択 → KPT を追加・管理:

| 種別 | 色 | 内容 |
|---|---|---|
| **Keep** | 緑 | うまくいっていること・継続したいこと |
| **Problem** | 赤 | 問題・課題・改善が必要なこと |
| **Try** | 青 | 次のスプリントで試すこと |

- カード単位で**インライン編集**と**削除**が可能
- **履歴タブ**: 全スプリントの KPT を横並びで一括確認

---

# 実績入力（ワークログ）

---

## ワークログ概要

**URL**: `/work-logs`

Outlook 風の**週次タイムラインカレンダー**で実績を管理:

- 縦軸: 時間軸（5:00〜22:00）
- 横軸: 曜日（月〜日）
- 土日・祝日は背景色でハイライト表示
- メンバーフィルタ: ドロップダウンで切り替え（デフォルト: ログインユーザー自身）

初期スクロール位置は「設定 → 一般」の**定時開始・終了**（デフォルト 10:00〜19:00）を中心に設定

---

## 実績の入力・編集

**新規登録**: カレンダーのグリッド上をドラッグして時間枠を選択するとモーダルが開く

| 入力項目 | 必須 |
|---|---|
| メンバー | ✅ |
| 実績種別（カテゴリ） | ✅ |
| 開始時刻 / 終了時刻 | ✅ |
| エピック | — |
| イシュー | — |
| 備考 | — |

**編集・削除**: 登録済みブロックをクリック → モーダルで編集・削除
**移動・リサイズ**: ドラッグで移動、下端ドラッグでリサイズ可能

---

## 工数集計の仕組み

実績種別のグループに「**工数あり**」フラグが設定されているカテゴリのみ工数集計対象。

- 「工数なし」グループ（会議・社内イベントなど）は記録されるが集計レポートには含まれない
- 種別・グループは `/settings/work-log-categories` で管理

スプリント詳細の Task 行には**ワークログ合計が実績(h)として自動反映**される。

---

# 勤怠管理

---

## 勤怠管理概要

**URL**: `/attendance`

週単位のカレンダーで個人の勤怠を記録:

| 種別 | 説明 |
|---|---|
| **全休** | 終日休み |
| **午前半休** | 午前中のみ休み |
| **午後半休** | 午後のみ休み |
| **早退** | 指定時刻以降早退（時刻入力必須） |
| **遅刻** | 指定時刻まで遅刻（時刻入力必須） |

各日付セルの「+」ボタンから入力。同じ日にすでに記録がある場合は上書き。

---

# 設定

---

## 設定メニュー一覧

| ページ | URL | 概要 |
|---|---|---|
| 一般 | `/settings/general` | 工数基準・外観・実績入力設定・リリースバッファ |
| リポジトリ | `/settings/repositories` | GitHub リポジトリの追加・同期設定 |
| メンバー | `/settings/members` | チームメンバーの表示名・稼働時間 |
| ラベル | `/settings/labels` | ベロシティ計算の管理対象ラベル設定 |
| 実績種別 | `/settings/work-log-categories` | ワークログ種別（カテゴリ）・グループを管理 |
| 祝日 | `/settings/holidays` | 日本の祝日・特別休日を管理 |

---

## 一般設定

**URL**: `/settings/general`

| 設定項目 | 説明 | デフォルト |
|---|---|---|
| 外観 | ライト / ダーク / システム連動 | システム |
| 1人日の基準時間 | 工数（h）→ 人日換算の基準 | 7時間 |
| 定時開始・終了 | ワークログカレンダーの初期スクロール位置 | 10:00〜19:00 |
| リリースバッファ日数 | due_date から開発完了目標日までの営業日数 | 0営業日 |
| GitHub ステータス優先度順 | エピック一覧の GitHub ステータス表示優先度リスト | 同期後自動設定 |

---

## ラベル管理 / 実績種別 / 祝日

**ラベル管理** `/settings/labels`
- GitHub 同期後に一覧表示
- トグルで `exclude_velocity` を設定（OFF にすると該当ラベルの Issue をベロシティ除外）
- 活用例: `spike`・`tech-debt`・`管理` などを除外

**実績種別管理** `/settings/work-log-categories`
- グループ（工数あり/なし）とカテゴリを管理
- テーブル行クリックでインライン編集

**祝日管理** `/settings/holidays`
- 「日本の祝日を自動取得」で一括インポート
- 手動追加で企業独自の特別休日を登録可能
- 着手日目安の営業日計算に使用される

---

## まとめ

```
セットアップ → リポジトリ追加 → GitHub 同期
  → スプリントに story_points 入力
  → Issue をエピックに紐付け
  → Task に予定工数入力
  → ワークログで実績を記録
  → バーンダウンで進捗確認
  → レトロスペクティブで振り返り
  → CSV エクスポートでクライアント報告
```

**週次フロー**: 同期 → ポイント更新 → ワークログ記録 → バーンダウン確認 → KPT 記録
